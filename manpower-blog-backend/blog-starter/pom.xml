<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.manpowergroup.blog</groupId>
        <artifactId>manpower-blog-backend-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>blog-starter</artifactId>
    <packaging>jar</packaging>
    <name>blog-starter</name>

    <dependencies>
        <!-- Web、Security、MyBatis-Plusを含むフレームワーク層を追加する -->
        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-framework</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 通用模块 -->
        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 配置提示（可选） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-module-system</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-admin-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-portal-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-member-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-module-content</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.manpowergroup.blog</groupId>
            <artifactId>blog-module-member</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- アーキテクチャテスト用（LayerDependencyTest）
             全モジュールに依存する本モジュールでのみ、レイヤ依存方向を横断的に検証できる -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 専用エンジン（archunit-junit5）は JUnit Platform 1.11 系でルールが検出されず
             テスト0件のまま成功扱いになるため、コア API を通常の JUnit テストから呼び出す方式を採る -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
